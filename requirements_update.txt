# Обновленные зависимости для DeltaDesk
# Добавьте строки в ваш requirements.txt (если их нет)

python-telegram-bot>=21.0
ccxt>=2.5.0
aiohttp>=3.8.0
python-dotenv>=0.19.0
numpy>=1.21.0

# DEX Сканирование (уже в aiohttp)
# aiohttp используется для GeckoTerminal API
